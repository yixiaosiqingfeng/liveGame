/**
 * 配置文件模板
 */

export default {
  // `baseURL` 将自动加在 `url` 前面，除非 `url` 是一个绝对 URL。
    // 本地测试地址：
      baseURL: 'http://db.com/api',
    //线上地址：
      //  baseURL: '',
  //图片请求地址前缀, 除非 `url` 是一个绝对 URL。
    // 本地测试地址：
      baseImgURL: 'http://db.com/upload/',
    //线上地址：
      //  baseImgURL: '',
}
